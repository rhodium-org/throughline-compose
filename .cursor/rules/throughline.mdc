---
description: throughline-compose / IDD requirements guidance
alwaysApply: true
---

Agent guidance for this repository is maintained in `AGENTS.md` at the repository
root — follow it. In short: run `tl-compose -C idd context` for the generated,
authoritative brief; drive everything through `tl-compose`, never bare `tl`;
ground every requirement upward before you build it; and **only a named human
ratifies** machine-proposed items.
